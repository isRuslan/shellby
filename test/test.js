var shellby = require('../')
  , assert = require("assert");

describe('Test `exec` command', function () {
  it('Should say `hello`', function () {
      // (shellby.exec('echo "hellp"')).should.equal("hello");
    // assert.equal("hello", shellby.exec('echo "hello"'));
  });
});
